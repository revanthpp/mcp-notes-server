# MCP Basics

The Model Context Protocol gives AI applications a standard way to discover and
use tools, resources, and prompts.

A host contains the user experience and model. A client maintains a connection
to one MCP server. The server describes capabilities and handles invocations.

MCP standardizes the exchange. It does not remove the need for authorization,
input validation, confirmation, or careful data boundaries.
